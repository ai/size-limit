{
  "name": "integration",
  "private": true,
  "devDependencies": {
    "@size-limit/preset-small-lib": ">= 0.0.0",
    "@size-limit/dual-publish": ">= 0.0.0",
    "dual-publish": ">= 0.0.0",
    "size-limit": ">= 0.0.0"
  },
  "size-limit": [
    {
      "limit": "1 B",
      "import": "{ VERY_LONG_NAME_FOR_CONST_TO_TEST_TREE_SHAKING }"
    }
  ]
}
